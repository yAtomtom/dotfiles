provider: claude
language: ja
model: 'claude-opus-5'
provider_options:
  claude:
    effort: max
  copilot:
    effort: high
# plan / plan-implement の command quality gate（plan-document-complete）の実行許可。
# 未設定だと command 型 quality gate はロード時にデフォルト拒否される
workflow_command_gates:
  custom_scripts: true
